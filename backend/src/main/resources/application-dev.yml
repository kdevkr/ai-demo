logging:
  level:
    com.example.demo: DEBUG
    org.springframework.web: DEBUG

# CORS 설정
cors:
  allowed-origins: 
    - http://localhost:3000
    - http://localhost:8080
  allowed-methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
  allowed-headers:
    - "*"